// Feature flags. Set PASSPORT_ENABLED back to true to bring the emergency health passport back
// everywhere at once: /passport, /scan/[id], and its links in the header, bottom bar and landing page.
export const PASSPORT_ENABLED = false;
